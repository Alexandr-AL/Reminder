<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Reminder.MainPage"
             xmlns:vm="clr-namespace:Reminder.ViewModels"
             xmlns:model="clr-namespace:Reminder.Models"
             x:DataType="vm:MainPageViewModel"
             Title="{Binding Title}">
    
    <Shell.TitleView>
        <Grid>
            <Button HorizontalOptions="End" 
                    Margin="0, 0, 10, 0" 
                    BackgroundColor="Transparent" 
                    ImageSource="plus.svg"
                    Command="{Binding AddNewEventCommand}"/>

            <Label VerticalOptions="Center"
                   FontFamily="Calibri" 
                   Text="Напоминалка"  
                   FontSize="20" 
                   TextColor="White" 
                   Margin="10, 0, 0, 0"/>
        </Grid>
    </Shell.TitleView>
    
    <Grid ColumnDefinitions="*" RowDefinitions="*">

        <ScrollView Grid.Column="0" Grid.Row="0">
            
            <VerticalStackLayout Margin="5">
                
                <CollectionView ItemsSource="{Binding Events}"
                                SelectedItem="{Binding}"
                                MaximumWidthRequest="350"
                                HorizontalOptions="Center">
                    
                    <CollectionView.ItemTemplate>
                        
                        <DataTemplate x:DataType="model:Event">
                            
                            <Frame BackgroundColor="Transparent"
                                   IsClippedToBounds="False"
                                   Margin="10,5,10,10" Padding="20,5,10,15">
                                
                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer CommandParameter="{Binding .}"
                                                          Command="{Binding Source={RelativeSource AncestorType={x:Type vm:MainPageViewModel}}, Path=EditEventCommand}"/>
                                </Frame.GestureRecognizers>

                                <VerticalStackLayout Margin="1">

                                    <Grid>
                                        <Label Text="{Binding Name}"
                                               FontFamily="Calibri" 
                                               FontSize="24"/>
                                        <ImageButton CommandParameter="{Binding .}"
                                                     Command="{Binding Source={RelativeSource AncestorType={x:Type vm:MainPageViewModel}}, Path=DeleteEventCommand}"
                                                     Source="plus01.png"
                                                     MaximumWidthRequest="25"
                                                     MaximumHeightRequest="25"
                                                     Opacity="0.4"
                                                     Rotation="45"
                                                     HorizontalOptions="End"/>
                                    </Grid>

                                    <Label Text="{Binding DateTimeEvent, StringFormat='{0:dd/MM/yyyy HH:mm}'}"
                                           FontSize="14"/>

                                    <Border BackgroundColor="Transparent" 
                                            Margin="0,1,0,0" 
                                            Padding="0,1,0,0"/>

                                    <Label Text="{Binding Description}" FontSize="16"/>

                                </VerticalStackLayout>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>
        </ScrollView>

        <ImageButton Source="plus01.png" 
                     Grid.Column="0"
                     Grid.Row="0"
                     HorizontalOptions="End" 
                     VerticalOptions="End"
                     MaximumHeightRequest="64"
                     MaximumWidthRequest="64"
                     Aspect="Fill"
                     Margin="0,0,30,30"
                     Opacity="0.6"
                     Command="{Binding AddNewEventCommand}"/>
        
    </Grid>
</ContentPage>