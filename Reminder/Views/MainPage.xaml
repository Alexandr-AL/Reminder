<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Reminder.MainPage"
             xmlns:vm="clr-namespace:Reminder.ViewModels"
             xmlns:model="clr-namespace:Reminder.Models"
             x:DataType="vm:MainPageViewModel"
             Title="{Binding Title}">

    <ScrollView >
        <Grid ColumnDefinitions="*"
              RowDefinitions="*">
        
            <VerticalStackLayout VerticalOptions="Start" HorizontalOptions="Center" Padding="10" Spacing="20">

                <Button Text="GetData" Command="{Binding GetDataEventsCommand}" Padding="50,10" HorizontalOptions="Center"/>
            
                <CollectionView ItemsSource="{Binding Events}" >
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="model:Event">
                            <VerticalStackLayout Padding="20,0">
                                
                                <Border Margin="0,10"/>
                                
                                <Label Text="{Binding Name}"/>
                                
                                <Label Text="{Binding DateTimeEvent}"/>
                                
                                <DatePicker Date="{Binding DateTimeEvent}" BackgroundColor="Beige"/>
                                
                                <TimePicker Time="{Binding DateTimeEvent.TimeOfDay}"/>
                                
                                <Label Text="{Binding Description}"/>
                                
                             </VerticalStackLayout>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            
            </VerticalStackLayout>
        </Grid>
    </ScrollView>
</ContentPage>
